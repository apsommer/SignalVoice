<resources>

    <!-- main theme -->
    <style name="Theme.SignalVoice" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- splash screen -->
    <style name="Theme.SignalVoice.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/app_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/app</item>
        <item name="postSplashScreenTheme">@style/Theme.SignalVoice</item>
    </style>
</resources>